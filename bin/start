#!/usr/bin/env node

const debug = require('debug')('mq-tea')

require('../index')
  .startServer({ port: 1883 })
  .listen(() => {
    debug('[%s] Server is up and running', process.env.NODE_ENV)
  })
